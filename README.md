<img width="1408" height="801" alt="image" src="https://github.com/user-attachments/assets/7998a3fc-f31c-45cb-af46-4fb86178df2a" />


# TeacherPilot: The AI Command Center for Educators

**Eliminating decision fatigue for overwhelmed teachers**

> "The AI did 100 things. You see 3."

---

## What is this?

TeacherPilot is a daily command center built for a real teacher managing 272 students across 13 groups. The core problem it solves: teachers drown in administrative noise - grading backlogs, parent emails, lesson prep, attendance flags - spread across multiple tools with no coherent picture of what actually matters *right now*.

Instead of yet another chat interface that requires constant prompt engineering, TeacherPilot surfaces exactly **3 prioritized actions per day**, generated by Mistral AI after analyzing the teacher's live schedule, pending tasks, and deadlines. The teacher sees only what needs to happen next, not a firehose of everything.

---

## Meet Marimba

Marimba is the AI companion at the heart of TeacherPilot - a fox character inspired by the founder's chocolate border collie, and named after the traditional Colombian instrument. Clever, loyal, gets things done.

Marimba is always present as a small avatar. The teacher can tap it or press a mic button to speak. Marimba responds in a consistent voice (ElevenLabs) and updates the screen accordingly. Responses are concise - never more than 2-3 sentences.

**Visual states:**

| State | Behavior |
|---|---|
| Idle | Subtle breathing / blinking animation (CSS) |
| Listening | Ears perk up, eyes widen, mic indicator visible |
| Thinking | Slight head tilt, animated dots or swirl |
| Speaking | Mouth open/close synced to audio volume |

**Example interactions:**
- *"What's my next class?"* → "You have 9A1 in the Codingspace at 11:30. Want me to prep it?"
- *"What's urgent?"* → "One thing: the coordinator needs a reply about Tuesday's event."
- *"Help me prep 6B2"* → Opens classroom card for 6B2 + reads briefing aloud

**Voice pipeline:**
```
Teacher speaks → Voxtral Realtime (transcription)
                        ↓
               Mistral Large 3 (reasoning)
                        ↓
             ElevenLabs (consistent voice ID)
                        ↓
              Marimba speaks + screen updates
```

The mic button is always accessible. Marimba's voice is always the same voice ID - not generic TTS, a real persona.

---

## Design philosophy

Two principles guide every decision in this project:

**Emotional connection over utility.** The interface is not a dashboard. It is a daily companion. Marimba greets the teacher by time of day, acknowledges the weight of the workload, and speaks in complete sentences - not bullet points or percentages. The goal is for a teacher to feel *supported*, not monitored.

**Cognitive ease over comprehensiveness.** The screen shows 3 things. Not 30. The schedule day is displayed as a quiet label - `Day 3` - not a progress bar, not a calendar widget. Information that doesn't reduce a decision is noise. Everything in the UI earns its place by reducing what the teacher has to think about.

> The hardest design constraint: nothing on screen should require explanation.

---

## How it works

```
Teacher's schedule + pending tasks
           |
           v
   Mistral Large 3 (AI reasoning)
           |
           v
  Top 3 prioritized actions
           |
           v
  Teacher sees 3 priority cards
           |
           v
   Tap a card → briefing + action
           |
           v
  Review, approve, mark done → next
```

The backend assembles a real task pool from four sources: **manually-added tasks** (teacher-typed or spoken), **action-required emails** (classified by Mistral Small from the Gmail feed), **today's meetings** extracted from the weekly announcements document, and **action items** from the same document. This pool is sent to Mistral Large 3, which returns the 3 most important tasks ranked by urgency and context - along with a one-sentence reason per task that surfaces as Marimba's note in the UI.

If no API key is configured, a local scoring algorithm ranks by priority level, due date, and today's schedule - so the app always works.

The current schedule day (1-6) is tracked with a persistent rolling calculator that correctly skips weekends and school holidays, rather than naively mapping Monday → 1. A manual override endpoint allows correcting drift caused by closures or cancelled days.

The weekly announcements (two emails that arrive every Friday) are pasted directly into a textarea in the UI. Mistral Small extracts meetings, class disruptions, and action items into the database. A passive reminder banner appears on Fridays until the content is pasted.

---

## Core User Flow

**Step 1 - Open**
Teacher opens TeacherPilot → Marimba greets them by time of day → 3 **priority cards** appear, each showing class, subject, estimated time, and urgency.

**Step 2 - Tap**
Teacher taps a card → it expands into a full briefing: attendance flags, class history, AI-suggested actions. The card itself becomes the workspace - no navigation away.

**Step 3 - Marimba handles it**
Marimba surfaces the pre-drafted output - graded rubric, email draft, lesson plan - right inside the card. Teacher reviews, tweaks if needed, and approves. Marimba reads the briefing aloud as the card opens.

**Step 4 - Voice (always available)**
Teacher speaks to Marimba at any time: *"What's urgent?"*, *"Help me prep 6B2"*, *"Mark that done."* Voxtral transcribes it, Mistral reasons about it, ElevenLabs speaks the response back. Marimba's visual state changes with what it's doing.

**Step 5 - Done**
Teacher marks the card done → card dismisses → next priority surfaces automatically → repeat until the day is handled.

> **Cards, not bullets.** Bullets were a list to read. Cards are a space to work in.

---

## Features

### Priority Cards (Home Screen)
Three AI-ranked cards shown at a time - no scrolling, no overwhelm. Each card shows class, subject, estimated time, urgency, and a one-sentence Marimba note explaining *why* this task is critical today. The pool is built from real sources only: user-added tasks, emails, and weekly announcement data.

### Manual Task Inbox
A compact section below the Top 3 where the teacher adds real tasks by typing (voice coming). Tasks persist in the database and immediately join the Mistral priority pool. Each task has a priority level and optional due date. One tap to delete when done.

### Session Log
Each classroom briefing card has a "Log this session" form. The teacher writes what happened and what worked well. The last session note is shown every time that class opens. The `what_worked` field is stored separately to enable cross-group suggestions in a future phase.

### Weekly Announcements
Two emails arrive every Friday with the week's schedule, meetings, class disruptions, and action items. The teacher pastes them into a textarea; Mistral Small extracts structured data into the database. A passive banner appears on Fridays as a reminder. Extracted meetings and action items automatically enter the priority pool.

### Marimba Voice Companion
Always-present fox avatar with four visual states (idle, listening, thinking, speaking). Voice input via Voxtral Realtime; spoken responses via ElevenLabs with a consistent voice persona. The mic button is always one tap away. ✅ Live

### Data Connectors

TeacherPilot pulls from the real tools the teacher already uses. No new habits required.

| Source | What lives there | Connector status |
|---|---|---|
| **Gmail (native OAuth2)** | Parent emails, coordinator requests | ✅ Live - Mistral Small classifies batches |
| **Weekly doc paste** | Meetings, disruptions, action items | ✅ Live - manual paste, Mistral Small extracts |
| **Google Sheets** | Full student list (272 students, 13 groups) | Stub - in progress |
| **Toddle** | Grades, assignments, and submission status | Stub - in progress |
| **Google Calendar** | School events, meetings, deadlines | Stub - in progress |

The connector layer is designed so each source is independent - a failure in one doesn't block the others.

### Rolling Schedule Day
The current day in the 6-day rotation is calculated from a persistent state file, correctly skipping weekends and holidays. The UI shows it as a quiet `Day N` label next to today's classes. On weekends it reads `Weekend` and the empty state says *"Enjoy the rest."*

### The Classroom Briefing Card
Tapping a class chip expands a briefing panel: absence flags, class history, last session notes (real from DB, falls back to rich placeholder), disruption warnings, and a "Plan lesson →" button. The teacher can log the session directly from the panel.

### Communication Triage
Emails classified as `action_required` surface in the priority pool and in the Inbox Tray. Absence notifications are extracted automatically and shown as red dots on class chips.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Frontend | React 18 + TypeScript, Vite |
| Styling | Tailwind CSS, Radix UI, class-variance-authority |
| Data fetching | TanStack React Query, Axios, Zod |
| Backend | Python 3.11, FastAPI, Uvicorn |
| Database | SQLite via SQLAlchemy (emails, absences, session logs, user tasks, weekly schedule) |
| AI (reasoning) | Mistral Large 3 (`mistral-large-latest`) |
| AI (triage) | Mistral Small (`mistral-small-latest`) - batch email classifier |
| AI (voice input) | Voxtral Realtime |
| AI (voice output) | ElevenLabs |
| Deployment | Docker Compose, Cloudflare Tunnel |
| Email | Native Gmail OAuth2 connector (`connectors/gmail.py`) |

---

## Project Structure

```
teacher-pilot-mistral/
├── docker-compose.yml           # One command to run everything
├── .env.example                 # Copy to .env and fill in secrets
├── backend/
│   ├── main.py                  # FastAPI app - all endpoints
│   ├── context_builder.py       # Builds the Mistral prompt (schedule + disruptions + meetings)
│   ├── mistral_client.py        # Mistral Large call → returns [{id, reason}] per priority
│   ├── schedule_day.py          # Rolling 6-day schedule calculator
│   ├── email_processor.py       # Pydantic models + batch triage logic
│   ├── database.py              # SQLAlchemy - 5 tables: emails, absences, weekly_schedule,
│   │                            #   class_sessions, user_tasks
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── connectors/              # Stubs for future data sources
│   │   ├── gmail.py
│   │   ├── calendar.py
│   │   ├── sheets.py
│   │   └── toddle.py
│   ├── prompts/
│   │   ├── email_triage.py      # Batch email classifier (Mistral Small)
│   │   └── weekly_schedule.py   # Weekly announcements extractor (Mistral Small)
│   ├── data/
│   │   ├── teacher_schedule.json     # 6-day rotating class schedule (source of truth)
│   │   ├── schedule_state.json       # Persistent rolling day state
│   │   └── teacher_pilot.db          # SQLite database (gitignored)
│   └── tests/
│       ├── conftest.py
│       ├── test_priorities.py        # 16 tests - scoring, Mistral path, fallback
│       ├── test_class_sessions.py    # 7 tests - session log CRUD + upsert
│       ├── test_email_processor.py   # 9 tests - triage, idempotency, edge cases
│       ├── test_schedule_day.py      # 7 tests - rolling calculator
│       └── test_weekly_schedule.py   # 6 tests - Mistral extraction
├── frontend/
│   ├── nginx.conf               # Serves SPA + proxies /api/* → backend
│   ├── Dockerfile
│   ├── src/
│   │   ├── components/
│   │   │   ├── ActiveCard.tsx        # Expanded priority task (with Marimba note)
│   │   │   ├── FridayBanner.tsx      # Friday reminder + weekly announcement paste panel
│   │   │   ├── InboxTray.tsx         # Email inbox view
│   │   │   ├── MarimbaGreeting.tsx   # Time-of-day greeting
│   │   │   ├── MarimbaWidget.tsx     # Avatar widget (bottom-right)
│   │   │   ├── PriorityCard.tsx      # Summary card (collapsed)
│   │   │   ├── PriorityList.tsx      # Top 3 grid + active card switcher
│   │   │   ├── TodaySchedule.tsx     # Class chips + briefing panel + session log
│   │   │   └── UserTaskSection.tsx   # Manual task inbox below Top 3
│   │   ├── lib/
│   │   │   ├── api/client.ts         # Axios client + Zod schemas for all types
│   │   │   ├── hooks/                # React Query hooks (priorities, schedule, absences,
│   │   │   │                         #   weekly schedule, class sessions, user tasks)
│   │   │   ├── mockBriefings.ts      # Placeholder data for priority card Marimba notes
│   │   │   ├── mockClassBriefings.ts # Per-group placeholder data (unit, last session, flags)
│   │   │   └── utils/dateUtils.ts    # Due date formatting helpers
│   │   └── App.tsx
│   ├── index.html
│   └── package.json
├── CONTRIBUTING.md
└── README.md
```

---

## Getting Started

### Option A - Docker (recommended for home server)

See the [Deploy](#deploy-home-server--cloudflare) section below.

### Option B - Local development

**Prerequisites:** Python 3.11+ and Node.js 22+

**Backend**

```bash
cd backend
cp ../.env.example ../.env   # fill in MISTRAL_API_KEY
pip install -r requirements.txt
python main.py
# → http://localhost:8000
```

**Frontend**

```bash
cd frontend
npm install
npm run dev
# → http://localhost:5173
```

No extra config needed - Vite automatically proxies `/api/*` requests to `localhost:8000`.

**API endpoints**

| Endpoint | Description |
|---|---|
| `GET /api/priorities` | Top 3 AI-ranked tasks - each includes `marimba_note` when Mistral is active |
| `GET /api/schedule` | Teacher's class schedule + current day number |
| `GET /api/schedule-day` | Current schedule day (1-6) |
| `POST /api/schedule-day` | Manual override - body: `{"date": "YYYY-MM-DD", "day": N}` |
| `POST /api/fetch-emails` | Trigger native Gmail connector to fetch and triage unread emails |
| `GET /api/absences` | All recorded student absences |
| `GET /api/important-emails` | Action-required emails |
| `POST /api/weekly-schedule` | Paste weekly announcement text - Mistral Small extracts it |
| `GET /api/weekly-schedule` | Latest extracted weekly schedule |
| `POST /api/tasks` | Add a teacher task - body: `{"title", "priority"?, "due_date"?}` |
| `GET /api/tasks` | List all teacher-added tasks |
| `DELETE /api/tasks/{id}` | Remove a teacher-added task |
| `POST /api/class/{group}/session` | Log a class session - body: `{"notes", "what_worked"?}` |
| `GET /api/class/{group}/last-session` | Most recent session log for a group |
| `GET /api/health` | Health check - includes Mistral config status |
| `GET /api/docs` | Interactive API docs (Swagger UI) |

> **Without a Mistral key:** priorities still work using a local scoring algorithm. `GET /api/health` shows `mistral.configured: false` so you can confirm the key is loaded.

**Running tests**

```bash
cd backend
pytest tests/ -v
```

46 tests across 5 test files: priority scoring and Mistral integration, class session CRUD, email triage, rolling schedule day, and weekly schedule extraction.

---

## Deploy (Home Server + Cloudflare)

The production setup runs entirely on a home server. A Cloudflare Tunnel exposes the app publicly without port forwarding or a static IP, routing `your-domain.com` straight to the server over an encrypted tunnel.

```
Internet → Cloudflare Edge → Tunnel (cloudflared) → nginx (frontend:80)
                                                          ↓
                                                   /api/* proxy
                                                          ↓
                                                  backend:8000
```

### One-time setup - Cloudflare Tunnel

1. Go to [Cloudflare Zero Trust](https://one.dash.cloudflare.com) → **Networks → Tunnels → Create a tunnel**
2. Choose **Cloudflared**, name it (e.g. `marimba-server`)
3. On the install screen, copy just the **token** (the long string after `--token`)
4. Add a **Public Hostname**: hostname `your-domain.com` → service type `HTTP`, URL `frontend:80`
5. Save

### Deploy

```bash
# On the server - first time
git clone <repo> teacher-pilot-mistral
cd teacher-pilot-mistral
cp .env.example .env
nano .env   # add MISTRAL_API_KEY and CLOUDFLARE_TUNNEL_TOKEN

# Build and launch everything (backend + frontend + tunnel)
docker compose up -d --build
```

All three containers start with `restart: unless-stopped`, which means:
- If a container crashes → Docker restarts it automatically
- If the server reboots → Docker starts on boot and brings everything back up
- The only way containers stay stopped is if you explicitly run `docker compose stop`

### Useful commands

```bash
docker compose logs -f          # stream logs from all containers
docker compose logs backend     # backend only
docker compose restart backend  # apply a backend change without rebuilding

# Pull latest code and rebuild
git pull && docker compose up -d --build

# Local access (bypasses Cloudflare - useful for debugging on the server)
# http://localhost:8080
```

---

## Environment Variables

Copy `.env.example` to `.env` at the project root and fill in values:

```
MISTRAL_API_KEY=your-mistral-api-key-here       # Required for AI prioritization, email triage, and STT
ELEVENLABS_API_KEY=your-elevenlabs-api-key      # Required for TTS
ELEVENLABS_VOICE_ID=your-elevenlabs-voice-id    # Required for TTS character voice
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here  # Required for production deployment
ENVIRONMENT=production                          # Optional - defaults to "development"
```

Without `MISTRAL_API_KEY` the app still works - priorities fall back to the local scoring algorithm and email triage is skipped. Without `CLOUDFLARE_TUNNEL_TOKEN` only local access is available.

---

## Common Issues

**Schedule day counter is wrong after a holiday or Easter break?**

The app tracks which day in the 6-day rotation it is using a rolling calculator. Long breaks (Easter, holidays, cancelled days) can cause it to drift. Fix it with one command — just tell the API what today's real day number is:

```bash
curl -X POST http://localhost:8000/api/schedule-day \
  -H "Content-Type: application/json" \
  -d '{"date": "2026-04-30", "day": 6}'
```

Replace `2026-04-30` with today's date and `6` with the correct day number (1–6). The rolling calculator picks up from there correctly.

If using Docker, the backend is accessible at `http://localhost:8000` from the server itself, or from `http://localhost:8080/api/schedule-day` through the nginx proxy.

---

**Port 8000 already in use?**
```bash
lsof -i :8000
kill -9 <PID>
# or run on a different port:
uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

---

## Security

- Never commit `.env` files - they are gitignored
- Use `.env.example` as the template (safe to commit, contains no secrets)
- All API keys are read from environment variables at runtime
- Report vulnerabilities responsibly before opening a public issue
