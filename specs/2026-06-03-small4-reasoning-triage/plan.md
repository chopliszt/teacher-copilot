# Plan — Small 4 reasoning for triage

Numbered task groups. Do them in order; each group ends at a checkpoint you can
verify before moving on. See `validation.md` for the merge gate.

---

## 1. Branch & dependency bump  ✅ (done at branch creation)

1.1 Create branch `experiment/small4-reasoning-triage`.
1.2 Bump `backend/requirements.txt`: `mistralai==1.12.4` → `mistralai==2.4.9`.
1.3 Install into the dev environment: `cd backend && pip install -r requirements.txt`.

**Checkpoint:** `python -c "import mistralai; print(mistralai.__version__)"` → `2.4.9`.

---

## 2. Discover what v2 actually changed (inspect before editing)

Per CLAUDE.md: inspect the real interface, never assume signatures from memory.

2.1 `python -m pydoc mistralai.Mistral` and inspect `chat.complete_async`
    signature, return type, and the message/content object shape.
2.2 Confirm `reasoning_effort` is an accepted kwarg in v2.
2.3 Inspect the tool-calling surface used by `task_chat.py` / `lesson_plan.py`
    (how tools are passed, how tool_calls come back).
2.4 Inspect `audio.transcriptions` for `stt.py`.

**Checkpoint:** a short written note of every v1→v2 delta that touches our 8 files.

---

## 3. Run the suite on v2 BEFORE editing — capture the breakage

3.1 `cd backend && pytest tests/ -v` against the freshly-upgraded SDK.
3.2 Record which tests fail and why (import errors, attribute errors, shape
    mismatches). This is the migration to-do list, generated by the suite itself.

**Checkpoint:** a list of failing tests mapped to the call site each implicates.

---

## 4. Migrate the non-reasoning call sites to v2 (mechanical sweep)

Fix only what v2 broke; do not change behavior.

4.1 `mistral_client.py` (priorities) — response parsing.
4.2 `prompts/voice.py` — response parsing.
4.3 `prompts/meeting_summary.py` — response parsing.
4.4 `stt.py` — `audio.transcriptions` surface.

**Checkpoint:** these four call sites import and run without error under v2.

---

## 5. Migrate the high-risk tool-calling sites (most care)

5.1 `prompts/task_chat.py` — tool definitions + tool_call response handling.
5.2 `prompts/lesson_plan.py` — same.
5.3 Verify tool-call round-trips still produce the same actions as before.

**Checkpoint:** `tests/test_voice_actions.py` and any task-chat / lesson-plan
tests pass; a manual tool-call smoke test produces the expected action.

---

## 6. Add reasoning to the Small-model sites (the actual feature)

6.1 `prompts/email_triage.py` — add `reasoning_effort="high"` to the
    `chat.complete_async` call. Parser (`_response_to_json_text`) is already
    chunk-safe; confirm it still extracts clean JSON from a v2 reasoning response.
6.2 `prompts/weekly_schedule.py` — add `reasoning_effort="high"`; verify its
    JSON parsing handles the thinking/text chunk split (port the same chunk
    handling if it currently assumes a plain string).

**Checkpoint:** triage + weekly-schedule return correct structured output with
reasoning enabled, on a live call.

---

## 7. Fix the SDK-shape test

7.1 `tests/test_weekly_schedule.py` hand-rolls a `FakeChoice` / `complete_async`.
    Update its fake response object to match the v2 shape (and the reasoning
    chunk list) so it exercises the real path.

**Checkpoint:** `pytest tests/test_weekly_schedule.py -v` passes.

---

## 8. Full suite green

8.1 `cd backend && pytest tests/ -v` — all tests pass under v2 (the 46-test
    baseline, adjusted for any tests touched here).

**Checkpoint:** zero failures. (Merge gate half #1.)

---

## 9. Eval: does reasoning actually help triage?

9.1 Run the triage evals against Small-3 baseline (`mistral-small-2506`, no
    reasoning) vs. Small-4 with `reasoning_effort="high"`.
9.2 Record the accuracy / precision / recall delta on the eval set.
9.3 Note latency and token-cost impact for awareness (informational).

**Checkpoint:** reasoning matches or beats the baseline. (Merge gate half #2.)
If it does NOT beat baseline, stop and reconsider before merging — the SDK bump
alone is not worth merging without the intelligence win.

---

## 10. Document & wrap

10.1 Update `backend/MODEL_REGISTRY.md` if model IDs / reasoning usage changed.
10.2 Update project memory (`mistral-reasoning-sdk.md`) with the final v2 call
     shape and any migration gotchas.
10.3 Summarize the eval delta in the PR description.

**Checkpoint:** PR ready, both merge-gate halves satisfied per `validation.md`.
